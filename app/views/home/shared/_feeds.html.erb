<% @FeedCourses.each do |course| %>
    <div class="bg-white border p-2 rounded-md mb-2">
        <h1 class="font-bold "><%= course.title %></h1>
        <p class=""><%= course.content %></p>
    </div>
<% end %> 